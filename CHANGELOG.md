# Changelog

[Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) 形式に概ね準拠し、
[セマンティックバージョニング](https://semver.org/lang/ja/) に従います。

バージョンはコミット履歴からさかのぼって付けたもので、タグは打っていません。

## [Unreleased]

### Added
- `CHANGELOG.md`（本ファイル）。コミット履歴からさかのぼって 6 つの版に整理した
- Backlog 課題クローンに「その他の操作」を足し、ツール側の `menu.py`（単純複製・
  直接更新）を開けるようにした。週次一括はランチャー側に残している。ツール側の
  メニューは 1 日ぶんしか扱わず、委譲すると月〜金の一括登録が失われるため

### Changed
- `.gitignore` を共通規約の定型に揃えた。資格情報のパターン（`.env` / `*.pem` /
  `*.key` / `credentials*` など）が抜けていた
- `CLAUDE.md` に本リポジトリ固有の事情を書いた。共通規約からの逸脱（`.vscode/` を
  丸ごとは無視しない、commit / push を確認なしで行う）、コミット前の検査、
  メニュー番号を変えない方針、ツール側メニューへの委譲の 4 つ
- `--log` の出力例から絶対パスを外した（`~/` 表記に統一）。共通規約の個人情報検査は
  プレースホルダのつもりのホームディレクトリ表記も検出するため、コミット不可の
  状態だった

## [0.6.0] - 2026-09-16

自前の対話メニューを持つツールが増えたため、そちらへ委譲する形に寄せました。

### Added
- docmold（Markdown → HTML 変換）をメニューに追加

### Changed
- ファイル同期チェックをツール側の `menu.py` に委譲。ランチャーが持っていた
  3 択（通常実行 / `--verbose` / `--retry 2`）は、ツール側の「条件を選んで実行」が
  包含している
- 自前メニューを持つツールの判定を、ラベル名ではなく `script == "menu.py"` に変更。
  同種のツールを足すたびに直さずに済む

## [0.5.0] - 2026-08-29

落ちない・自動実行できる・検証できる、の 3 点を整えた版です。

### Added
- 無人実行。ツール番号に続けてサブメニューの選択を渡すと入力待ちなしで動く
  （`menu.py 3 2 1`）。cron やタスクスケジューラから使える
- `--yes`。無人実行で破壊的操作を承認する。指定が無ければ実行せずエラー終了する
- 番号での直接起動（`menu.py 4`）、`--help` / `--list` / `--log`
- 実行ログ。実行したコマンドと終了コードを直近 50 件記録する（`--log` で表示）
- 前回実行したツールを記憶し、空 Enter で再実行できるようにした
- `tools.yaml`。`menu.py` を編集せずにツールを追加できる
- pytest 一式（日付計算・バッチ制御・安全弁・引数の受け渡し）
- `README.md`、`requirements.txt` / `requirements-dev.txt`

### Changed
- ツールに `.venv` があればその Python で実行するようにした。ツール名では分岐せず
  `.venv` の有無だけで判断する
- 単純なツールを宣言で定義できるようにし、手書きハンドラ 4 つを畳んだ。組み込みと
  `tools.yaml` が同じ仕組みで動く
- 終了コードをツールのものそのまま返すようにした。直接起動の成否を判定できる

### Fixed
- ツールが未配置のときランチャーごと落ちていた（`FileNotFoundError`）。メッセージを
  出してメニューに戻る
- `Ctrl+C` / `Ctrl+D` でトレースバックが出ていた
- 金・土・日に「今週」を選ぶと 1 件も登録しないまま「完了しました」と表示していた
- 無人実行で `--execute` だけを渡していたため、子ツールが非対話環境を検知して
  全件スキップし、成功扱いで終わっていた。承認済みのときは `--yes` も渡す
- サブメニューで戻っただけのツールが「前回」として記録されていた

## [0.4.0] - 2026-05-22

### Added
- ファイルリスト生成（filelist）をメニューに追加
- docgrep（ファイル全文検索）をメニューに追加

### Changed
- docgrep をツール側の `menu.py` に委譲。検索条件の組み立てはツール側に置いた

## [0.3.0] - 2026-05-14

### Added
- ファイル同期チェック（file_sync_checker）をメニューに追加

## [0.2.0] - 2026-04-09

### Added
- Backlog 課題クローンの週次登録。今週・来週・再来週から選び、月〜金の 5 日分を
  1 日ずつ登録する

### Changed
- 「今週」を選んだとき、当日以前をスキップするようにした

## [0.1.0] - 2026-04-04

### Added
- メニュー形式のツール起動スクリプト（`menu.py`）
- Backlog 週次レポート生成、Excel → Backlog 課題登録

### Changed
- バッチ / PowerShell での起動をやめ、VSCode から起動する形にした
